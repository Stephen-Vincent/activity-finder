/**
 * Formatting helpers.
 *
 * - formatDistance(meters, unit): "1.2 mi" or "2.0 km"
 * - formatPrice(amount, currency): "£8.50" or "€9.00"
 * - formatAgeRange(min, max): "3–7 yrs", "All ages", "From 8"
 * - formatOpenStatus(hours, now): "Open · closes 17:00", "Closed · opens Sat 09:00"
 *
 * All locale-aware and unit-aware. Currency follows the venue's location,
 * not the user's. Distance unit follows the user's preference.
 */

export {};
