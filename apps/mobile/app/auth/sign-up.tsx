/**
 * Sign-up screen.
 *
 * Same providers as sign-in. Captures display name + marketing opt-in.
 * Onboarding (children, home postcode) happens on /onboarding immediately
 * after first sign-in.
 */

export default function SignUpScreen() {
  // TODO: implement.
  return null;
}
