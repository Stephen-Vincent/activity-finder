/**
 * Sign-in screen.
 *
 * Methods:
 *  - Email magic link (Supabase Auth).
 *  - Apple Sign In (required by App Store policy if any social login is offered).
 *  - Google Sign In.
 *
 * On success, redirect to /onboarding if profile is incomplete, else /(tabs)/home.
 */

export default function SignInScreen() {
  // TODO: implement.
  return null;
}
