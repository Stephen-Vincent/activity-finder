/**
 * Profile tab.
 *
 * Sections:
 *  - Account (display name, email, avatar).
 *  - Children (add / edit / remove child profiles — DOB, nickname, notes).
 *  - Preferences (currency display, distance unit, notification settings).
 *  - Premium (status, manage subscription via Stripe portal).
 *  - Venue owner (if user owns one or more venues, link to web admin).
 *  - Legal (privacy policy, terms, contact).
 *  - Sign out.
 *  - Delete account (cascades, see docs/DESIGN.md §10).
 */

export default function ProfileScreen() {
  // TODO: implement.
  return null;
}
