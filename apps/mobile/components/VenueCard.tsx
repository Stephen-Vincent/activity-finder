/**
 * VenueCard
 *
 * Compact card used in feeds, search results, plan items.
 * Shows: hero image, name, distance, age range badge, price band, indoor/outdoor icon,
 * favourite toggle, and (for Pro venues) a small "Featured" badge.
 *
 * Props: { venue: Venue, distanceMeters?: number, onPress, onFavouriteToggle }
 */

export default function VenueCard() {
  // TODO: implement.
  return null;
}
