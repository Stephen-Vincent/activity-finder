/**
 * useChildren
 *
 * Reads the signed-in user's children. Provides:
 *   - list of children with computed `ageYears` (recomputed daily)
 *   - addChild, updateChild, removeChild mutations
 *
 * Children data is private — RLS ensures only the owning user can read.
 */

export {};
