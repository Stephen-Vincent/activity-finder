/**
 * useVenues
 *
 * TanStack Query hook returning venues for the current viewport / filter set.
 * Wraps lib/queries.ts venuesWithin().
 *
 * Variants:
 *   - useVenues(filters)               // discover feed, infinite
 *   - useVenue(idOrSlug)               // detail screen
 *   - useNearbyVenues(centre, radius)  // home + map
 */

export {};
