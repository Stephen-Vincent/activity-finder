/**
 * useFavourites
 *
 * Read + toggle favourite venues for the signed-in user.
 *
 * Free tier: capped at 25 favourites (Parent Premium = unlimited). Cap is
 * enforced server-side (RLS + check on insert) and shown in the UI.
 */

export {};
